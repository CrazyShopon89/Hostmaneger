# 🚀 HostMaster AI - cPanel Deployment Guide

## 🇧🇩 cPanel-এ সেটআপ করার নিয়ম

### ১. ফাইল আপলোড
আপনার সব ফাইল cPanel-এর `public_html` বা নির্দিষ্ট ডোমেইন ফোল্ডারে আপলোড করুন।

### ২. Node.js অ্যাপ তৈরি
- cPanel সার্চ বারে লিখুন **"Setup Node.js App"**।
- **Create Application** ক্লিক করুন।
- **Node.js version**: ১৮ বা তার বেশি সিলেক্ট করুন।
- **Application mode**: `production` সিলেক্ট করুন।
- **Application root**: আপনার ফোল্ডারের নাম (যেমন `hostmaster`)।
- **Application URL**: আপনার ডোমেইন সিলেক্ট করুন।
- **Application startup file**: `server.js` লিখুন।
- **Create** বাটনে ক্লিক করুন।

### ৩. এনভায়রনমেন্ট ভেরিয়েবল (API_KEY)
- অ্যাপ সেটিংসে নিচের দিকে **Environment variables** সেকশনে যান।
- **Add Variable** ক্লিক করে **Name**: `API_KEY` এবং **Value**: `আপনার_গেমিনি_কি` দিন।
- **Name**: `PORT` এবং **Value**: `8080` দিন।
- **Save** করুন।

### ৪. মডিউল ইন্সটল (npm install)
- cPanel থেকে **Terminal** ওপেন করুন।
- "Setup Node.js App" পেজে থাকা **Command for entering the virtual environment** লাইনটি কপি করে টার্মিনালে পেস্ট করুন এবং এন্টার দিন।
- এরপর লিখুন: `npm install`
- সবশেষে cPanel থেকে অ্যাপটি **Restart** দিন।

## 🛠️ প্রোডাকশন টিপস
- **500 Error:** যদি অ্যাপ কাজ না করে, তবে `.htaccess` ফাইলটি চেক করুন। আমি এখানে একদম সেইফ একটি কোড দিয়েছি।
- **SQLite:** আপনার ডেটা `hostmaster.db` ফাইলে থাকবে। এটি ব্যাকআপ রাখতে পারেন।
- **SMTP:** ইমেইল কাজ না করলে **Settings** থেকে পোর্ট ৪৬৫ বা ৫৮৭ চেক করে দেখুন এবং `SSL/TLS` ব্যবহার করুন।